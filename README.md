# Zinc

> **No stable release is available yet.**

This is the `stable` branch of the Zinc compiler. It is reserved for released,
production-ready versions of the language and toolchain.

There is currently nothing stable to ship. Active development — including the
in-progress prototype — lives on other branches (see `prerelease` and `main`).

Please check back later for the first stable release.
