struct Point {
    x: int
    y: int
    func getDistanceFromOrigin(): float {
        return sqrt(this.x * this.x + this.y * this.y)
    }
}

func main(): int {
    print("Hello World")
    const p: Point
    p.x = 3
    p.y = 4
    print("Distance from origin: ", p.getDistanceFromOrigin())
}