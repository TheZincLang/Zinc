// ── Zinc import target ──────────────────────────────────────────────────
// Provides a few exported symbols for testFiles/main.zn to import.

export const PI: double = 3.14159

export fn square(x: int): int {
    return x * x
}

export fn cube(x: int): int {
    return x * square(x)
}
